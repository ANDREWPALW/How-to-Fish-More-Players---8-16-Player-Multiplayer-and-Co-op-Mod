# How To Fish More Players v2.1.1

16-player BepInEx 5 plugin for How To Fish.

Build fix in v2.1.1: the project compiles only `src/Plugin.cs`, so an old duplicate `Plugin.cs` left in the repository root cannot cause duplicate-member compiler errors.
